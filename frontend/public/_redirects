# Cloudflare Pages SPA 重定向配置
# 所有非文件路径的请求都重定向到 index.html，让 Vue Router 处理路由

# API 代理（如果需要）
# /api/*  https://your-api.com/:splat  200

# 静态资源直接访问（不重定向）
/assets/*  /assets/:splat  200

# 所有其他路径重定向到 index.html
/*    /index.html   200
