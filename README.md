# 文件管理系统 - 商业级 SaaS

基于 Vue 3 + Supabase 的现代化文件管理系统，支持图片和 Markdown 文件的上传、管理和预览。

## ✨ 功能特性

- 🔐 用户认证系统（注册/登录）
- 📤 文件上传（支持拖拽）
- 🖼️ 图片管理
- 📝 Markdown 文件管理
- 👁️ Markdown 在线预览
- ⬇️ 文件下载（签名 URL）
- 🔒 行级安全策略（RLS）
- 🗑️ 文件删除
- 📱 响应式设计

## 🏗️ 技术栈

### 前端
- Vue 3 - 渐进式 JavaScript 框架
- Vue Router - 官方路由管理器
- Pinia - 状态管理
- Vite - 下一代前端构建工具
- Axios - HTTP 客户端
- Markdown-it - Markdown 渲染器

### 后端
- Supabase - 开源 Firebase 替代方案
  - PostgreSQL 数据库
  - 认证系统
  - Storage 存储
  - 行级安全策略（RLS）

## 📦 项目结构

```
project-root/
├── frontend/                   # Vue 前端项目
│   ├── src/
│   │   ├── api/               # API 接口
│   │   │   ├── supabase.js    # Supabase 客户端
│   │   │   ├── auth.js        # 认证 API
│   │   │   └── files.js       # 文件 API
│   │   ├── components/        # Vue 组件
│   │   │   ├── FileUploader.vue
│   │   │   ├── FileList.vue
│   │   │   └── MarkdownViewer.vue
│   │   ├── pages/             # 页面组件
│   │   │   ├── Login.vue
│   │   │   ├── Dashboard.vue
│   │   │   ├── Upload.vue
│   │   │   └── Preview.vue
│   │   ├── router/            # 路由配置
│   │   ├── stores/            # Pinia 状态管理
│   │   ├── utils/             # 工具函数
│   │   ├── App.vue
│   │   ├── main.js
│   │   └── style.css
│   ├── index.html
│   ├── vite.config.js
│   ├── package.json
│   └── .env.example
│
└── backend/                   # Supabase 配置
    ├── sql/                   # 数据库脚本
    │   ├── 01_tables.sql      # 表结构
    │   ├── 02_rls.sql         # 安全策略
    │   └── 03_seed.sql        # 种子数据
    ├── storage-rules/         # Storage 配置
    │   └── private-files.json
    └── README.md
```

## 🚀 快速开始

### 1. 后端设置（Supabase）

1. 访问 [supabase.com](https://supabase.com) 创建新项目
2. 在 SQL Editor 中依次执行 `backend/sql/` 目录下的脚本
3. 在 Storage 中创建名为 `private-files` 的 bucket（设置为私有）
4. 配置 Storage 策略（参考 `backend/README.md`）

### 2. 前端设置

```bash
# 进入前端目录
cd frontend

# 安装依赖
npm install

# 复制环境变量文件
cp .env.example .env

# 编辑 .env 文件，填入 Supabase 配置
# VITE_SUPABASE_URL=your-project-url
# VITE_SUPABASE_ANON_KEY=your-anon-key

# 启动开发服务器
npm run dev
```

### 3. 访问应用

打开浏览器访问 `http://localhost:3000`

## 📝 使用说明

### 注册/登录
1. 首次使用需要注册账户
2. 输入邮箱和密码（密码至少 6 位）
3. 如果启用了邮箱验证，需要验证邮箱

### 上传文件
1. 点击"上传文件"按钮
2. 选择或拖拽文件到上传区域
3. 支持的文件类型：
   - 图片：jpg, png, gif, webp
   - Markdown：.md, .markdown
4. 文件大小限制：50MB

### 管理文件
- 查看文件列表
- 下载文件
- 预览 Markdown 文件
- 删除文件

## 🔒 安全特性

- ✅ 用户认证和会话管理
- ✅ 行级安全策略（RLS）- 用户只能访问自己的数据
- ✅ 私有 Storage bucket - 需要签名 URL 访问
- ✅ 文件路径包含用户 ID - 防止路径猜测
- ✅ 文件大小和类型验证
- ✅ 路由守卫 - 未登录自动跳转

## 🛠️ 开发命令

```bash
# 开发模式
npm run dev

# 构建生产版本
npm run build

# 预览生产构建
npm run preview
```

## 📚 API 文档

### 认证 API
- `login(email, password)` - 用户登录
- `register(email, password)` - 用户注册
- `logout()` - 用户登出
- `getUser()` - 获取当前用户

### 文件 API
- `uploadFile(file, type)` - 上传文件
- `getFiles()` - 获取文件列表
- `getFileById(id)` - 获取单个文件
- `getSignedUrl(path)` - 获取签名 URL
- `deleteFile(id, path)` - 删除文件

## 🎨 自定义样式

项目使用原生 CSS，所有样式都在组件的 `<style scoped>` 中定义。可以根据需要修改颜色、间距等。

主要颜色变量：
- 主色：`#4299e1` (蓝色)
- 成功：`#48bb78` (绿色)
- 错误：`#e53e3e` (红色)
- 背景：`#f7fafc` (浅灰)

## 📄 许可证

MIT License

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

## 📧 联系方式

如有问题，请提交 Issue。
